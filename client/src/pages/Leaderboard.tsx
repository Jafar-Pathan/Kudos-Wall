import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Skeleton } from "@/components/ui/skeleton";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, ArrowUpRight, Medal, Sparkles, Trophy } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";
import { toast } from "sonner";
import { useAuth } from "@/_core/hooks/useAuth";

const hues = ["bg-coral", "bg-gold", "bg-lilac", "bg-mint", "bg-sky"];
function initials(name?: string | null, fallback = "NW") { return name?.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase() || fallback; }

export default function Leaderboard() {
  const [department, setDepartment] = useState<string>("all");
  const { user } = useAuth();
  const query = trpc.leaderboard.list.useQuery({ department: department === "all" ? undefined : department as "Engineering" | "Design" | "Marketing" | "Sales" });
  const rows = query.data ?? [];
  const hasData = rows.length >= 3;
  return <div className="min-h-screen bg-paper text-ink"><div className="mx-auto max-w-[1040px] px-5 py-8 md:px-10 md:py-12"><div className="flex flex-wrap items-center justify-between gap-4"><div><Button asChild variant="ghost" className="mb-6 -ml-3 rounded-xl px-3 text-ink/50 hover:bg-ink/5 hover:text-ink"><Link href="/"><ArrowLeft className="size-4" />Back to wall</Link></Button><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-coral">Recognition, ranked</p><h1 className="mt-2 font-display text-4xl font-bold tracking-tight md:text-5xl">The leaderboard<span className="text-coral">.</span></h1><p className="mt-3 max-w-xl text-sm leading-6 text-ink/55">A snapshot of the teammates making the biggest impact this month. Recognition earned, not self-reported.</p></div><div className="flex items-center gap-2"><Select value={department} onValueChange={setDepartment}><SelectTrigger className="h-10 w-[172px] rounded-xl border-ink/15 bg-white"><SelectValue placeholder="All departments" /></SelectTrigger><SelectContent><SelectItem value="all">All departments</SelectItem><SelectItem value="Engineering">Engineering</SelectItem><SelectItem value="Design">Design</SelectItem><SelectItem value="Marketing">Marketing</SelectItem><SelectItem value="Sales">Sales</SelectItem></SelectContent></Select><Button onClick={() => user ? toast.success("Keep recognizing the people around you.") : (window.location.href = "/auth")} className="hidden rounded-xl bg-ink text-paper hover:bg-ink/90 sm:flex"><Sparkles className="size-4" />Give kudos</Button></div></div>{hasData ? <div className="mt-12 grid gap-4 md:grid-cols-3"><div className="relative overflow-hidden rounded-2xl bg-lilac/65 p-6 md:order-2 md:mt-8"><div className="absolute -right-8 -top-8 size-28 rounded-full border-[18px] border-white/20" /><div className="flex items-center gap-2 text-xs font-bold text-ink/50"><Medal className="size-4" />2nd place</div><div className="mt-8 grid size-16 place-items-center rounded-full border-4 border-white bg-gold text-sm font-bold shadow-sm">{initials(rows[1]?.name)}</div><p className="mt-4 font-display text-2xl font-bold">{rows[1]?.name}</p><p className="text-xs text-ink/45">{rows[1]?.department}</p><p className="mt-6 font-display text-3xl font-bold">{rows[1]?.earnedPoints}<span className="ml-1 text-sm font-sans font-semibold text-ink/40">pts</span></p></div><div className="relative overflow-hidden rounded-2xl bg-ink p-6 text-paper md:order-1"><Trophy className="absolute right-6 top-6 size-6 text-gold" /><div className="flex items-center gap-2 text-xs font-bold text-paper/55"><Medal className="size-4 text-gold" />1st place</div><div className="mt-8 grid size-20 place-items-center rounded-full border-4 border-coral bg-gold text-base font-bold text-ink shadow-sm">{initials(rows[0]?.name)}</div><p className="mt-4 font-display text-2xl font-bold">{rows[0]?.name}</p><p className="text-xs text-paper/45">{rows[0]?.department}</p><p className="mt-6 font-display text-4xl font-bold text-coral">{rows[0]?.earnedPoints}<span className="ml-1 text-sm font-sans font-semibold text-paper/40">pts</span></p></div><div className="relative overflow-hidden rounded-2xl bg-mint/70 p-6 md:order-3 md:mt-8"><div className="flex items-center gap-2 text-xs font-bold text-ink/50"><Medal className="size-4" />3rd place</div><div className="mt-8 grid size-16 place-items-center rounded-full border-4 border-white bg-coral text-sm font-bold text-white shadow-sm">{initials(rows[2]?.name)}</div><p className="mt-4 font-display text-2xl font-bold">{rows[2]?.name}</p><p className="text-xs text-ink/45">{rows[2]?.department}</p><p className="mt-6 font-display text-3xl font-bold">{rows[2]?.earnedPoints}<span className="ml-1 text-sm font-sans font-semibold text-ink/40">pts</span></p></div></div> : <div className="mt-12 rounded-2xl border border-dashed border-ink/15 bg-white p-10 text-center"><Trophy className="mx-auto size-8 text-gold" /><p className="mt-4 font-display text-xl font-bold">No one on the podium yet.</p><p className="mt-1 text-sm text-ink/45">Be the first to recognize a teammate and help them reach the leaderboard.</p></div>}<section className="mt-10 overflow-hidden rounded-2xl border border-ink/10 bg-white"><div className="flex items-center justify-between border-b border-ink/10 px-5 py-5 md:px-7"><div><p className="font-display text-xl font-bold">Everyone showing up</p><p className="mt-1 text-xs text-ink/45">Top recognized employees · September 2026</p></div><Badge variant="outline" className="rounded-lg border-ink/10 bg-paper text-[10px] text-ink/45">Updated live</Badge></div>{query.isLoading ? <div className="space-y-3 p-6"><Skeleton className="h-14 rounded-xl" /><Skeleton className="h-14 rounded-xl" /><Skeleton className="h-14 rounded-xl" /></div> : query.isError ? <div className="p-10 text-center"><p className="font-bold">Leaderboard unavailable.</p><Button className="mt-4 rounded-xl bg-ink text-paper" onClick={() => query.refetch()}>Retry</Button></div> : rows.length === 0 ? <div className="p-10 text-center"><p className="font-bold">No recognition in this department yet.</p><p className="mt-1 text-sm text-ink/45">Be the first to change that.</p></div> : <div className="divide-y divide-ink/8">{rows.map((row, index) => <div key={row.id} className="flex items-center gap-4 px-5 py-4 transition hover:bg-paper/70 md:px-7"><span className="w-5 text-center font-display text-lg font-bold text-ink/25">{String(index + 1).padStart(2, "0")}</span><Avatar className={`size-10 border-2 border-white ${hues[index % hues.length]}`}><AvatarFallback className="bg-transparent text-[11px] font-bold text-ink">{row.avatar || initials(row.name)}</AvatarFallback></Avatar><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{row.name}</p><p className="text-[11px] text-ink/45">{row.department}</p></div><div className="hidden text-right sm:block"><p className="text-[10px] uppercase tracking-wider text-ink/35">Recognition earned</p><p className="text-sm font-bold">{row.earnedPoints} points</p></div><ArrowUpRight className="size-4 text-ink/25" /></div>)}</div>}</section></div></div>;
}
